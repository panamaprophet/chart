export type ChartCoordinate = [
    x: number,
    y: number
];

export type CanvasSize = {
    width: number,
    height: number,
};

export type Bounds = [
    leftBound: number,
    rightBound: number
];
